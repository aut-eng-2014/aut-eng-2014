#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#define N 50

char* stergere(char* s,int n,int p)
{
    if (p>strlen(s)) return s;
    char* aux;
    aux=(char*) malloc(strlen(s)-p)
}
int main()
{
char s[NR];
int n,p;
scanf("%d %d",&n,&p);
scanf("%s",s);
stergere(s,n,p);
printf("%s",s);
return 0;
}j

